import React from 'react';
import ReactDOM from 'react-dom';
import DrumMachineContainer from './containers/drumMachineContainer';

ReactDOM.render(<DrumMachineContainer />, document.getElementById('root'));